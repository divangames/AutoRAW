<UserControl x:Class="AutoRAW.Controls.BusySpinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="36"
             Height="36"
             SnapsToDevicePixels="True"
             IsHitTestVisible="False">
  <UserControl.Resources>
    <Storyboard x:Key="SpinStoryboard" RepeatBehavior="Forever">
      <DoubleAnimation Storyboard.TargetName="Ring"
                       Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                       From="0"
                       To="360"
                       Duration="0:0:0.85" />
    </Storyboard>
  </UserControl.Resources>
  <UserControl.Triggers>
    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
      <BeginStoryboard Storyboard="{StaticResource SpinStoryboard}" />
    </EventTrigger>
  </UserControl.Triggers>
  <Grid>
    <Ellipse x:Name="Ring"
             Width="32"
             Height="32"
             HorizontalAlignment="Center"
             VerticalAlignment="Center"
             Stroke="{DynamicResource Theme.Accent}"
             StrokeThickness="3"
             StrokeDashArray="6 18"
             Fill="Transparent"
             RenderTransformOrigin="0.5,0.5">
      <Ellipse.RenderTransform>
        <RotateTransform Angle="0" />
      </Ellipse.RenderTransform>
    </Ellipse>
  </Grid>
</UserControl>
