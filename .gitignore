bin/
obj/
.vs/
*.user
*.suo
*.cache
packages/
*.DotSettings.user
launchSettings.json
AutoRAW-launch.log
AutoRAW-build.log

# Bundled .NET SDK installer for Inno Setup (download locally; GitHub limit 100 MB per file)
installer/dotnet-sdk-*.exe

# Сборки и локальные бинарники (релизы — через GitHub Releases)
dist/
droples/
droplets/*.exe

# Кэш IDE
*.lscache

# Тяжёлые тестовые RAW (только локально; не коммитить в GitHub)
test/Many/
test/One/

# Adobe Photoshop — исходники макетов (в репозитории держим экспорт PNG/JPG)
*.psd

# ONNX-модели (скачать bat/download-yolov8n-onnx.ps1)
models/**/*.onnx

# Деплой презентации (SSH), локальные секреты
presentation/deploy.env
